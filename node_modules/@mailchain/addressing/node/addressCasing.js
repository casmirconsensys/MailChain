'use strict';

var consts = require('./protocols/consts.js');

function casingByProtocol(value, protocol) {
    switch (protocol) {
        case consts.MAILCHAIN:
            return value.toLowerCase(); // case insensitive
        case consts.ETHEREUM:
            return value.toLowerCase(); // case insensitive
        case consts.SUBSTRATE:
        case consts.TEZOS:
            return value; // substrate encoding is case sensitive
        case consts.ALGORAND:
            return value.toLowerCase(); // case insensitive
        case consts.NEAR:
            return value.toLowerCase();
        default:
            throw new Error(`casing for protocol [${protocol}] not defined`);
    }
}

exports.casingByProtocol = casingByProtocol;
