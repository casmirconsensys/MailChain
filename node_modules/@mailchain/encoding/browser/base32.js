import { base32Decode, base32Encode } from '@polkadot/util-crypto';

function decodeBase32(input) {
    return base32Decode(input);
}
function encodeBase32(input) {
    return base32Encode(input);
}

export { decodeBase32, encodeBase32 };
