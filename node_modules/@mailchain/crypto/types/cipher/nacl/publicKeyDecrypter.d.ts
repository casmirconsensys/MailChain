import { EncryptedContent, PlainContent, Decrypter } from '@mailchain/crypto/types/cipher/cipher';
import { KeyExchange } from '@mailchain/crypto/types/cipher/keyExchange';
import { PrivateKey } from '@mailchain/crypto/types/private';
export declare class PublicKeyDecrypter implements Decrypter {
    private _keyEx;
    private _prvKey;
    constructor(keyEx: KeyExchange, prvKey: PrivateKey);
    static FromPrivateKey(key: PrivateKey): PublicKeyDecrypter;
    decrypt(input: EncryptedContent): Promise<PlainContent>;
}
