import { RandomFunction } from '@mailchain/crypto/types/rand';
import { PrivateKey } from '@mailchain/crypto/types/private';
import { SECP256K1PublicKey } from '@mailchain/crypto/types/secp256k1/public';
export declare const PrivateKeyLen = 32;
export declare class SECP256K1PrivateKey implements PrivateKey {
    bytes: Uint8Array;
    readonly publicKey: SECP256K1PublicKey;
    readonly curve: string;
    constructor(bytes: Uint8Array);
    static generate(rand?: RandomFunction): SECP256K1PrivateKey;
    sign(message: Uint8Array): Promise<Uint8Array>;
}
