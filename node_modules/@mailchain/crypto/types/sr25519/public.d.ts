import { PublicKey } from '@mailchain/crypto/types/public';
export declare const SR25519PublicKeyLen = 32;
export declare class SR25519PublicKey implements PublicKey {
    readonly bytes: Uint8Array;
    readonly curve: string;
    constructor(bytes: Uint8Array);
    verify(message: Uint8Array, sig: Uint8Array): Promise<boolean>;
}
export declare function asPublicKey(key: PublicKey): SR25519PublicKey;
