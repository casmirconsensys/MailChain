import { mnemonicToLegacySeed } from '@polkadot/util-crypto/mnemonic';

function toSeed(mnemonic, password, byteLength) {
    return mnemonicToLegacySeed(mnemonic, password, true, byteLength);
}

export { toSeed };
